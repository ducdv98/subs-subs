{
  "name": "subs-subs",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "build:extension": "tsc app/domain/settings/settings.ts --outDir extension/lib --module ES2022 --target ES2022 --moduleResolution Bundler --skipLibCheck && tsc app/domain/subtitle-engine/subtitle-engine.ts --outDir extension/lib --module ES2022 --target ES2022 --moduleResolution Bundler --skipLibCheck"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
